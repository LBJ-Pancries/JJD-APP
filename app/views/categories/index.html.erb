<div class="col-md-12">
  <p>微民宿三期项目共7个户型：A1、A2、A3、B1、B2、B3、C1，共计38户。</p>
	<div class="group">
		<%= link_to("New category", new_category_path, class: "btn btn-primary pull-right") %>
	</div>
	<table class="table table-hover">
		<thead>
			<tr>
				<td>ID</td>
				<td>Title</td>
				<td>Description</td>
			</tr>
		</thead>
		<tbody>
			<% @categories.each do |category| %>
				<tr>
					<td><%= category.id %></td>
					<td><%= link_to(category.name, category_path(category))%></td>
					<td><%= category.description %></td>
					<td>
						<%= link_to("Edit", edit_category_path(category), class: "btn btn-sm btn-default")%>
						<%= link_to("Delete", category_path(category), class:"btn btn-sm btn-default", method: :delete, data: { confirm: "Are you sure?"})%>
					</td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>
