<div class="col-md-4 col-md-offset-4">
  <h2>修改户型</h2>
  <hr>
  <% if @category.image.present? %>
    <span>目前图片</span>
    <br>
    <%= image_tag(@category.image.thumb.url) %>
  <% end %>
  
  <%= render "form" %>
</div>
